[package]
name = "mrml-tensor"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "MRML Tensor: quantized tensor math, GGUF execution, and CUDA acceleration"

[features]
default = ["std"]
std = []
cuda = ["std"]

[dependencies]
mrml-math = { path = "../mrml-math" }

[[bench]]
name = "hot_paths"
harness = false
